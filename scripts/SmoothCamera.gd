# res://scripts/SmoothCamera.gd
# Edit file: res://scripts/SmoothCamera.gd
extends SpringArm3D

# The SpringArm3D automatically follows its parent (the Angel node).
# We do not need any code in _process() to manage its position.
# All camera settings should be adjusted in the Inspector.

func _process(_delta):
    pass
